[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --alluredir=allure-results --clean-alluredir
markers =
    e2e: end-to-end coverage for full user journeys
    smoke: fast coverage of core user journeys
    negative: expected validation and rejection scenarios
